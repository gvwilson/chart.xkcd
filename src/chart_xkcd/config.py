"""Shared constants for chart.xkcd."""


class positionType:
    """Legend and tooltip position constants matching the JS library.

    Pass one of these values as the ``legendPosition`` option to
    control where the legend is placed relative to the chart area.
    """

    upLeft = 1
    upRight = 2
    downLeft = 3
    downRight = 4
